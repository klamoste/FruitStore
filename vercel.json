{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "buildCommand": "python manage.py migrate --noinput && USE_DATABASE_URL=false STATIC_ROOT=.vercel_static python manage.py collectstatic --noinput",
  "functions": {
    "api/index.py": {
      "includeFiles": "{fruit_store/**,templates/**,.vercel_static/**,db.sqlite3}"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
